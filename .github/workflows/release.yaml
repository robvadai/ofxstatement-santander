name: release
on:
  workflow_run:
    workflows:
    - test
    branches:
    - main
    - dev
    types:
    - completed
jobs:
  release:
    name: Release the repository
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: 0
    - uses: cocogitto/cocogitto-action@v4.2.0
      with:
        command: bump
        args: --auto --skip-ci ${{ github.ref_name == 'dev' && ' --pre' || '' }}
        git-user: github-action
        git-user-email: 'github-actions@github.com'
